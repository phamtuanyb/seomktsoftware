import { Module } from '@nestjs/common';

/**
 * Placeholder — Task 1.4 fills in JwtStrategy, AuthController, AuthService,
 * password reset flow, and email verification.
 */
@Module({})
export class AuthModule {}
