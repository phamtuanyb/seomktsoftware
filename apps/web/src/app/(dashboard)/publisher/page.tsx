import { ComingSoon } from '@/components/shared/coming-soon';

export const metadata = { title: 'Xuất bản — MKT SEO AI' };

export default function PublisherPage() {
  return (
    <ComingSoon
      title="WordPress Auto Publisher"
      sprint="Sprint 6"
      spec="Section 8 — TN8"
      description="Adapter pattern (mở rộng Shopify/Haravan Phase 2). Connect site < 60s, publish bài (ảnh + schema + meta) < 10s, auto-detect Yoast/RankMath/SEOPress."
    />
  );
}
