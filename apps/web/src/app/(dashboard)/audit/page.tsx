import { ComingSoon } from '@/components/shared/coming-soon';

export const metadata = { title: 'Chấm điểm SEO — MKT SEO AI' };

export default function AuditPage() {
  return (
    <ComingSoon
      title="Content Score 0-100"
      sprint="Sprint 5"
      spec="Section 8 — TN7"
      description="12 rule (Chain of Responsibility), auto-fix cho rule <80 dùng AI. Realtime update mỗi 5s trong editor."
    />
  );
}
