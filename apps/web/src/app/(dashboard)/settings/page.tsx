import { ComingSoon } from '@/components/shared/coming-soon';

export const metadata = { title: 'Cài đặt — MKT SEO AI' };

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Cài đặt tài khoản"
      sprint="Sprint 2 — Task 2.3"
      spec="Section 9"
      description="Đổi mật khẩu, đổi email, MFA (Pro+), API keys, xóa tài khoản (GDPR)."
    />
  );
}
